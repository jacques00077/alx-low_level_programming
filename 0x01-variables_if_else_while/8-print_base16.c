#include <stdio.h>
/**
 * main - New Entry point
 * Return: 0
 */
int main(void)
{
	int k;

	for (k = 48; k < 58; k++)
	{
		putchar(k);
	}
	for (k = 97; k < 103; k++)
	{
		putchar(k);
	}
	putchar('\n');
	return (0);
}
