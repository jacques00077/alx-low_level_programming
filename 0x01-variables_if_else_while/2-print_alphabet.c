#include <stdio.h>
/**
 * main - A program that prints the alphbets in lowercas by using putchar
 * Return: 0 (success)
 */

int main(void)
{
	char y;

	y = 'a';
	while
		(y <= 'z') {
			putchar(y);
			y++;
		}
	putchar('\n');
	return (0);
}
